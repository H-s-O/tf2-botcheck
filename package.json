{
  "scripts": {
    "postinstall": "patch-package && npm rebuild",
    "copy-addons": "node scripts/copy-addons.js",
    "build": "npm run copy-addons && nexe index.js -t 12.13.0 -o build/tf2-botcheck.exe -r data/bots.json"
  },
  "dependencies": {
    "robotjs": "^0.6.0",
    "sleep": "^6.3.0"
  },
  "devDependencies": {
    "cpy": "^8.1.1",
    "nexe": "^3.3.7",
    "patch-package": "^6.2.2"
  }
}
